# Networks blocked outright (403). http context, so this covers every vhost.
#
# 43.172.0.0/15 — ACEVILLE PTE.LTD. (SG). A scraper farm: on 2026-08-03 it sent
# 152,665 requests, 64% of the day's traffic, from 1,794 distinct IPs behind
# rotating fake desktop Chrome user-agents — up 7x from 21,600 the day before.
# It paces at ~4 req/IP/minute, well under any per-IP threshold worth setting,
# so the /15 is the smallest key that reaches it. It ignores robots.txt. No
# human traffic was observed from the range.
deny 43.172.0.0/15;

# 47.79.0.0/16, 47.82.0.0/16, 43.119.0.0/16 — Alibaba Cloud. One operator
# running two arms: 47.79 fetches HTML from 651 IPs behind rotating spoofed
# Chrome user-agents, and 47.82 + 43.119 fetch only style.css/custom.js with
# self-referers from 1,274 more IPs — a headless browser rendering what the
# first arm crawls. Combined 11,114 requests on 2026-09-07, peaking at 20,592
# the day before. 47.79 stamps a fabricated "https://www.google.com/" referer
# on 7,550 of its 7,643 requests; the giveaway is identical malformed URLs
# (/games/176303/%20Report%5D&quot%3B%7D — a link harvested out of an
# HTML-escaped JSON blob) replayed from two different IPs three seconds apart.
# Not one image or favicon was requested across all 11,114, so no browser ever
# loaded a whole page: no human traffic to lose. Like ACEVILLE it paces well
# under any per-IP threshold, so the /16 is the smallest key that reaches it.
# 43.119 is registered ASEPL-SG; 47.79 and 47.82 sit in Alibaba's ARIN AL-3
# block, which whois does not place in Singapore.
deny 47.79.0.0/16;
deny 47.82.0.0/16;
deny 43.119.0.0/16;

# 45.148.10.0/24 (RIPE/NL) — scanners. .200 was blocked alone on 2026-09-07
# after 1,686 requests probing for an exposed site backup (/1.zip,
# /backup-2.zip, /archive_3.zip and so on), all 404. By 2026-09-11/12 ten
# siblings had joined it — .9, .18, .123, .166 and others — sending 9,226
# requests of WordPress /wp-json/batch/v1 POSTs and .env hunting, each of
# which 301s into a Django-rendered 404. Nothing here has ever been a page
# view, so the whole /24 goes.
deny 45.148.10.0/24;

# Twelve /24s, one operator — the /24s rise and fall in lockstep. 12,945
# requests on 2026-09-17, 18,202 on the 20th, 33,133 on the 21st, from ~3,000
# IPs; 27k of 88k over ten days were client aborts. It rotates Safari/Mac,
# Chrome/146 Linux and Chrome/13x Windows UAs, each sending headers consistent
# with its browser, and loads static files, so fake-chrome.conf passes all of
# it; it also follows up from the same ranges with UA-less requests. Nothing
# else came from these ranges. Where whois names an owner it is "Private
# Customer" (HK and EU registrations): proxy space, not people.
deny 46.18.90.0/24;
deny 87.82.253.0/24;
deny 87.83.12.0/24;
deny 87.85.235.0/24;
deny 87.86.188.0/24;
deny 143.14.6.0/24;
deny 143.20.253.0/24;
deny 144.31.35.0/24;
deny 146.103.54.0/24;
deny 162.141.68.0/24;
deny 207.180.11.0/24;
deny 216.75.132.0/24;

# 185.177.72.0/24 (FR-FBW-NETWORKS) — scanners. Three IPs sent 12,701
# requests as curl/8.7.1 on 2026-09-21, all probing for .env files and site
# backups, all 404.
deny 185.177.72.0/24;
